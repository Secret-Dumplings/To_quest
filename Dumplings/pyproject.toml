[project]
name = "dumplings"
version = "0.1.0"
description = "Dumplings Agent Framework"
requires-python = ">=3.12"
dependencies = [
    "loguru>=0.7.0",
    "beautifulsoup4>=4.12.0",
    "lxml>=5.0.0",
    "requests>=2.31.0",
    "mcp>=1.0.0",
]

[tool.uv]
dev-dependencies = []